# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
PackageIdentifier: swigerb.MediaMatch
PackageVersion: "${VERSION}"
Installers:
  - Architecture: x64
    InstallerUrl: "https://github.com/swigerb/MediaMatch/releases/download/v${VERSION}/MediaMatch-${VERSION}-x64.msix"
    InstallerType: msix
    InstallerSha256: "${SHA256}"
  - Architecture: arm64
    InstallerUrl: "https://github.com/swigerb/MediaMatch/releases/download/v${VERSION}/MediaMatch-${VERSION}-arm64.msix"
    InstallerType: msix
    InstallerSha256: "${SHA256_ARM64}"
ManifestType: installer
ManifestVersion: 1.6.0
