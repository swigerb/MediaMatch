<Page
    x:Class="MediaMatch.App.Web.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Sticky title -->
        <TextBlock Grid.Row="0" Text="Settings" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,16" />

        <!-- Notification bar -->
        <InfoBar Grid.Row="1" x:Name="SettingsNotification" IsOpen="False" IsClosable="True" Margin="0,0,0,8" />

        <!-- Settings content -->
        <ScrollViewer Grid.Row="2">
            <StackPanel Spacing="24" MaxWidth="600">

                <!-- API Keys Section -->
                <TextBlock Text="API Keys" Style="{ThemeResource SubtitleTextBlockStyle}" />

                <TextBox Header="TMDb API Key" x:Name="TmdbKeyBox"
                         PlaceholderText="Enter your TMDb API key" />

                <TextBox Header="TVDb API Key" x:Name="TvdbKeyBox"
                         PlaceholderText="Enter your TVDb API key" />

                <TextBox Header="OpenSubtitles API Key" x:Name="OpenSubsKeyBox"
                         PlaceholderText="Enter your OpenSubtitles API key" />

                <!-- Appearance Section -->
                <TextBlock Text="Appearance" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,16,0,0" />

                <ComboBox Header="Theme" x:Name="ThemeComboBox" SelectedIndex="0" Width="200">
                    <ComboBoxItem Content="System Default" />
                    <ComboBoxItem Content="Light" />
                    <ComboBoxItem Content="Dark" />
                </ComboBox>

                <!-- Actions -->
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,16,0,0">
                    <Button Content="Save" Style="{ThemeResource AccentButtonStyle}" Click="Save_Click" />
                    <Button Content="Clear Cache" Click="ClearCache_Click" />
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
